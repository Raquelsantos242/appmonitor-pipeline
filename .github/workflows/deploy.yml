name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
      - name: Simulate Deploy
        run: echo "Deploying to ${{ vars.PROD_DOMAIN || secrets.PROD_DOMAIN }}"
